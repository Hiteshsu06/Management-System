// hooks
import React from 'react';

// components
import Login from '@components/Login';

const LoginPage = () => {
  return (
    <React.Fragment>
      <Login/>
    </React.Fragment>
  )
}

export default LoginPage;